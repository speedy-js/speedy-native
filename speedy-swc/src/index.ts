import { Plugin } from "./plugins";
import { TransfromOutput } from './types'



export function transfrom(code: string, options?: {}, beforePlugins?: Plugin[], afterPlugins?: Plugin[]): TransfromOutput {

  return {
    code: 'TODO',
  }
}